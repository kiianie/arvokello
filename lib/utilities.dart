// Logiikka sanojen järjestämiseen ja vertailutulosten tallentamiseen

// import 'package:flutter/material.dart';

void getIndex(list) {

  int i = 0;

  for (i = 0; i < list.length - 1; i += 2) {
    // Check if there are at least two values to compare
    if (i >= list.length) {
      return;
    }
    //String firstValue = list[i];
    //String secondValue = list[i+1];

  }

  // You can also return these values or use them as needed
}